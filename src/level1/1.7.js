/**
 * Write a program that takes a list of strings as input and sorts the list in alphabetical order.
 * @param {array} arr
 * @returns sorts the list in alphabetical order
 */

function listAlphabeticalOrder(arr) {
    arr.sort();
    return arr; 
}

module.exports = listAlphabeticalOrder;