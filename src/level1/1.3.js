/**
 * Write a program that takes a number as input and displays its square.
 * @param {number} a
 * @returns displays its square
 */
function squareNum(a) {
  return a * a;
}
module.exports = squareNum;
